export type theme = "pink" | "blue" | "yellow" | "white";
export type logo_list = string;
export type photo_list = [string, string, string, string]
export type eventInfo = {
    name: string;
    date: string;
    ID:string;
    owner:string;
};